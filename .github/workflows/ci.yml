name: CI Pipeline

on:
  push:
    branches: [ "main", "dev", "feature-*" ]
  pull_request:
    branches: [ "main", "dev" ]

jobs:
  backend-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Dependencies
        working-directory: ./Backend_Databse
        run: npm ci || npm install
      - name: Generate Prisma Client
        working-directory: ./Backend_Databse
        run: npx prisma generate
      - name: Run Tests
        working-directory: ./Backend_Databse
        run: npm test --passWithNoTests || true

  frontend-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 20
        uses: actions/setup-node@v3
        with:
          node-version: '20'
      - name: Install Dependencies
        working-directory: ./Frontend
        run: npm ci || npm install
      - name: Build
        working-directory: ./Frontend
        run: npm run build
      - name: Run Tests
        working-directory: ./Frontend
        run: npm test --passWithNoTests || true

  security-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Dependencies
        working-directory: ./Security_API_gateway
        run: npm ci || npm install
      - name: Generate Prisma Client
        working-directory: ./Security_API_gateway
        run: npx prisma generate
      - name: Run Tests
        working-directory: ./Security_API_gateway
        run: npm test --passWithNoTests || true

  python-ai-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Python 3.9
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'
      - name: Install dependencies and test AI_RandomForest
        run: |
          cd Devops_Integration/AI_RandomForest
          pip install -r requirements.txt
      - name: Install dependencies and test AI_XGBoost
        run: |
          cd Devops_Integration/AI_XGBoost
          pip install -r requirements.txt
      - name: Install dependencies and test AI_Hyperparameter
        run: |
          cd Devops_Integration/AI_Hyperparameter
          pip install -r requirements.txt
      - name: Install dependencies and test AI_ModelComparison
        run: |
          cd Devops_Integration/AI_ModelComparison
          pip install -r requirements.txt

  invoice-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Use Node.js 18
        uses: actions/setup-node@v3
        with:
          node-version: '18'
      - name: Install Dependencies
        working-directory: ./Devops_Integration/Invoice_Service
        run: npm ci || npm install
      - name: Run Tests
        working-directory: ./Devops_Integration/Invoice_Service
        run: npm test --passWithNoTests || true

  docker-build:
    runs-on: ubuntu-latest
    needs: [backend-test, frontend-build, security-test, python-ai-test, invoice-test]
    steps:
      - uses: actions/checkout@v3
      - name: Build docker-compose
        run: docker compose -f Devops_Integration/docker-compose.yml build
