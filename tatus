[1mdiff --git a/Backend_Databse/src/app.js b/Backend_Databse/src/app.js[m
[1mindex 68432fe..180b9e8 100644[m
[1m--- a/Backend_Databse/src/app.js[m
[1m+++ b/Backend_Databse/src/app.js[m
[36m@@ -15,6 +15,7 @@[m [mconst analyticsRoutes    = require("./routes/analytics.routes");[m
 const invoiceRoutes      = require("./routes/invoice.routes");[m
 const forecastRoutes     = require("./routes/forecast.routes");[m
 const notificationRoutes = require("./routes/notification.routes");[m
[32m+[m[32mconst userRoutes         = require("./routes/user.routes");[m
 [m
 const app = express();[m
 [m
[36m@@ -58,6 +59,7 @@[m [mapp.use("/api/analytics",  authenticate, analyticsRoutes);[m
 app.use("/api/invoices",       authenticate, invoiceRoutes);[m
 app.use("/api/forecast",       authenticate, forecastRoutes);[m
 app.use("/api/notifications",  authenticate, notificationRoutes);[m
[32m+[m[32mapp.use("/api/users",          authenticate, userRoutes);[m
 [m
 // ── Centralised error handler ─────────────────────────────────────────────────[m
 app.use((err, req, res, next) => {[m
[1mdiff --git a/Security_API_gateway/src/app.js b/Security_API_gateway/src/app.js[m
[1mindex dd2bd62..31734f4 100644[m
[1m--- a/Security_API_gateway/src/app.js[m
[1m+++ b/Security_API_gateway/src/app.js[m
[36m@@ -30,6 +30,7 @@[m [mconst invoicesRoutes         = require("./routes/invoices.routes");[m
 const forecastRoutes         = require("./routes/forecast.routes");[m
 const notificationsRoutes    = require("./routes/notifications.routes");[m
 const auditSummaryRoutes     = require("./routes/auditSummary.routes");[m
[32m+[m[32mconst usersRoutes            = require("./routes/users.routes");[m
 [m
 // AI microservice routes — one file per service, one port per service[m
 const customerGroupsRoutes   = require("./routes/customerGroups.routes");[m
[36m@@ -111,6 +112,7 @@[m [mapp.use("/api/invoices",  authenticate, authorize, apiLimiter, invoicesRoutes);[m
 app.use("/api/forecast",       authenticate, authorize, apiLimiter, forecastRoutes);[m
 app.use("/api/notifications",   authenticate, authorize, apiLimiter, notificationsRoutes);[m
 app.use("/api/audit-summary",   authenticate, authorize, apiLimiter, auditSummaryRoutes);[m
[32m+[m[32mapp.use("/api/users",           authenticate, authorize, apiLimiter, usersRoutes);[m
 [m
 // ── AI microservice routes ────────────────────────────────────────────────────[m
 // Each mounts on its own dedicated router → its own AI service port.[m
[1mdiff --git a/Security_API_gateway/src/middleware/authorize.js b/Security_API_gateway/src/middleware/authorize.js[m
[1mindex fac395f..1dbfd5f 100644[m
[1m--- a/Security_API_gateway/src/middleware/authorize.js[m
[1m+++ b/Security_API_gateway/src/middleware/authorize.js[m
[36m@@ -42,6 +42,9 @@[m [mconst permissions = {[m
         notifications:        ["GET"],[m
         "audit-summary":      ["GET"],[m
 [m
[32m+[m[32m        // User management — Store Manager can view users[m
[32m+[m[32m        users:                ["GET"],[m
[32m+[m
         // AI insight services[m
         forecast:             ["GET"],[m
         "customer-groups":    ["GET", "POST"],[m
